# Figures directory

Populated automatically by src/eda.py, src/evaluate_models.py, and
src/explainability.py. See each script's docstring for which figure
numbers (as referenced in the report) correspond to which file.
